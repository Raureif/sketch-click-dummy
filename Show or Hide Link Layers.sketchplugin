// (shift control cmd h)

#import 'library.js'

var didShowLayers = toggleLinkLayers();

if (didShowLayers === true){
  doc.showMessage('Link layers shown.');
} else {
  doc.showMessage('Link layers hidden.');
}